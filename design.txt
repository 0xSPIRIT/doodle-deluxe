draw-and-guess game
-------------------

# Frontend handles:
1. Drawing interaction through a canvas, sends updated brushstrokes to server
   - Send the brushstrokes to server who broadcasts it to the other connected websockets
   - Send the strokes in batches & if buffer limit is not reached within 1s,
     dump the queue
   - Fill bucket
2. Receives state changes from backend:
   - Current player list
   - Player left
   - Player joined
   - Player sends an answer
   - Turn finished, transfer to next player

# Backend handles:

x Add player to room
x Broadcast messages to players
x Remove player from room
x When player joins, request canvas state from a user and send to newly joined player
  (we don't store the canvas state on the server)

- Deploy the backend to an Amazon EC2 instance

TODO:

x Request canvas state upon new player
x Create logic for the guessing & the round in general
x Cycle players when finished their round
x Only allow game to begin with >= 2 players
x Display error message if having the same username
- Add timer to the rounds
- Different rooms, creation and removal
- If room is empty, delete all data associated with it
- Add colors, fill bucket, brush size
- Make your own better frontend
